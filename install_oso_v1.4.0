yum install -y epel-release
yum clean all
yum install -y docker wget git ansible python-cryptography pyOpenSSL.x86_64
git clone https://github.com/openshift/openshift-ansible
git clone https://github.com/chenrygithub/oso_install
ssh-keygen -f /root/.ssh/id_rsa -t rsa -N ''
